Execute the handed-off canonical todo work for this orchestrated story.

Use the attached todo/spec/acceptance state, obey command-policy if present,
and finish with the required `STEP_*` line protocol only.

$ARGUMENTS
