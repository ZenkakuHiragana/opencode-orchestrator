Probe whether a list of candidate commands is available and runnable in the current environment.
Return a single JSON object with status and per-command results, following the
structure defined in the preflight-runner agent instructions.

Commands to check (JSON payload is injected here):
$ARGUMENTS
