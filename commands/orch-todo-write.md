Update the canonical todo plan for this task.

Prefer incremental replanning over full regeneration:

- add new todos when only new work is needed,
- update existing todos when the structure is mostly sound,
- replace the full canonical set only when the todo cache is missing, invalid, or unsalvageable against the current requirements.

Use the current requirements, spec, command policy, existing todo state, status hints, and open proposals to produce the smallest coherent todo-structure changes.

Todo-planning context:
$ARGUMENTS
