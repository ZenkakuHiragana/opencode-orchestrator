Use the approved Discovery Packet as the contract input for this orchestrated
story, then clarify requirements and maintain the acceptance index, spec, and
command policy.

Treat the approved Discovery Packet as the source of scope and decisions unless
the user explicitly changes it. Ask targeted questions only when needed to turn
that packet into acceptance-index, spec, and related summaries that todo-writer,
executor, and auditor can rely on. Surface any implied obligations that the
story or repository context makes necessary so they become explicit
requirements instead of hidden assumptions.

Expect the handoff context to identify, in explicit terms:

- the task key for this story,
- the current goal,
- the approved Discovery Packet,
- any current artifacts or recent changes that affect refinement.

If any of those inputs are missing or inconsistent, treat that as a handoff gap
and resolve it before you rewrite scope assumptions.

High-level goal and context:
$ARGUMENTS
